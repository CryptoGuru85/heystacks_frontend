const sitemap = require('./sitemap')
const serveHTML = require('./serve-html')
const serveDocHTML = require('./serve-doc-html')
const commentHTML = require('./comments')
const docHTML = require('./doc-html')
const emailHTML = require('./email-html')
const spaceHTML = require('./space-html')

module.exports = {
  sitemap,
  serveHTML,
  serveDocHTML,
  commentHTML,
  docHTML,
  emailHTML,
  spaceHTML
}
